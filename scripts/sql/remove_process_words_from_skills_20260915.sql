-- 【実行しないこと】「運用保守」「ヘルプデスク」を skill_master から外す案（2026-09-15）。
-- **一度実行したが同日に取り消した。** 経緯と実測値を残すためだけのファイル。
-- DELETE は末尾にコメントアウトして置いてある。
--
-- ■ きっかけ
--   営業から「スキル欄に 運用保守・ヘルプデスク が出るのはおかしい」（2026-09-15）。
--   確かにこの2語は **役割（ROLE_DEFS）にもスキル（skill_master）にも**登録されていて、
--   同じ記述から2つの軸で加点されていた。
--     役割   同一 +15（role_affinity）
--     スキル 重み 1（methodologies）
--
--   実測（prod・2026-09-15）:
--     語           案件が必須に指定  人材のスキル  人材の役割   両方持ち
--     運用保守            1件        2,051人      1,615人     1,440人
--     ヘルプデスク         3件          428人        714人       356人
--
-- ■ なぜ取り消したか
--   1. **2語だけ消しても概念は残る。** skill_master には「運用」「保守運用」の行が別にあり、
--      同じ記述をそのまま拾う。消えるのは表記の一部だけで、二重加点は止まらない。
--   2. **methodologies カテゴリ全体が同じ性質。** テスト 86,807 / 要件定義 73,606 など、
--      40数行がいずれも工程語で、案件側がスキルとして要求することはほぼ無い。
--      2語だけ外すと、同じ性質の語の中で扱いが不揃いになるだけ。
--   3. つまりこれは **カテゴリ全体の設計の話**であって、行の削除で解く問題ではない。
--
-- ■ 戻した手順（記録）
--   ・上記2行を category・aliases ごと再 INSERT
--   ・削除→再挿入で 0 に戻った match_count を元値に復元（運用保守 38170 / ヘルプデスク 25556）
--     ※ match_count=0 は quality_check が「AI由来のゴミ」を見分ける印。0 のままだと毎回削除候補に挙がる
--   ・node scripts/export_skill_master.mjs で辞書を再出力（952件）
--
-- ■ 積み残し（ユーザー判断待ち）
--   methodologies を (a) このまま置く / (b) 人材カードのスキル表示からだけ隠す /
--   (c) マッチングのスキル一致から外す のどれにするか。
--   候補者の 69% が持つ語は絞り込みの役に立たないので、少なくとも表示の整理はした方がよい。

-- delete from skill_master where name in ('運用保守', 'ヘルプデスク');

-- 現状確認（これは実行してよい）
select name, category, match_count from skill_master
 where name in ('運用保守', 'ヘルプデスク') order by name;
