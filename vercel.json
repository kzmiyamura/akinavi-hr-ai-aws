{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/analyze.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/docs/(.*)", "destination": "/docs/$1" },
    { "source": "/((?!docs/).*)", "destination": "/" }
  ]
}
